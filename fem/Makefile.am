# don't follow the full GNU-standard; we definitely need automake >= 1.5
AUTOMAKE_OPTIONS = foreign 1.5

AM_CPPFLAGS= 

# do everything in the subdirs
SUBDIRS = pass space discretefunction quadrature operator solver io misc
