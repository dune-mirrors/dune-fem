# IGNORE_HEADERS

LDADD = $(ALL_PKG_LDFLAGS) $(ALL_PKG_LIBS) -lblas

noinst_PROGRAMS = laplace sinelaplace
laplace_SOURCES = laplace.cc
sinelaplace_SOURCES = sinelaplace.cc

GRIDTYPE=ALBERTAGRID
GRIDDIM=2
POLORDER=1

AM_CPPFLAGS = $(ALL_PKG_CPPFLAGS) -D$(GRIDTYPE) -DGRIDDIM=$(GRIDDIM) -DPOLORDER=$(POLORDER) -Wall -g
#AM_CPPFLAGS = $(ALL_PKG_CPPFLAGS) -D$(GRIDTYPE) -DGRIDDIM=$(GRIDDIM) -DPOLORDER=$(POLORDER) -Wall -O3 -unroll -DNDEBUG

