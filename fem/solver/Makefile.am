# don't follow the full GNU-standard; we definitely need automake >= 1.5
AUTOMAKE_OPTIONS = foreign 1.5

SUBDIRS = ode oemsolver 

solverdir =  $(includedir)/solver
solver_HEADERS =  istlsolver.hh odesolver.hh oemsolver.hh
