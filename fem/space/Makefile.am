# don't follow the full GNU-standard; we definitely need automake >= 1.5
AUTOMAKE_OPTIONS = foreign 1.5

SUBDIRS = test common test dgspace lagrangespace combinedspace basefunctions
spacedir =  $(includedir)/dune/fem/space
space_HEADERS = dgspace.hh lagrangespace.hh combinedspace.hh
