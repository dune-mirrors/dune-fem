POLORD=2
LDADD = $(ALL_PKG_LDFLAGS) $(ALL_PKG_LIBS)
CPPFLAGS = $(ALL_PKG_CPPFLAGS) -DPOLORDER=$(POLORD)
if ALBERTA
ADAPTPROG = l2pro adapt
endif

noinst_PROGRAMS = $(ADAPTPROG)
l2pro_SOURCES = l2projection.cc
if ALBERTA
adapt_SOURCES = adapt.cc
endif
