POLORD=0
GRIDTYPE = ALBERTAGRID
DUNE_WORLD_DIM = $(DUNE_PROBLEM_DIM)

LDADD = $(ALL_PKG_LDFLAGS) $(ALL_PKG_LIBS) $(MPI_LDFLAGS)
AM_CPPFLAGS = $(ALL_PKG_CPPFLAGS) -DPOLORDER=$(POLORD) -D$(GRIDTYPE) -DGRIDDIM=$(DUNE_PROBLEM_DIM) -DGRIDDIMWORLD=$(DUNE_WORLD_DIM) -DNDEBUG -O3 -unroll


if ALBERTA
ADAPTPROG = l2pro adapt
endif

noinst_PROGRAMS = $(ADAPTPROG)
l2pro_SOURCES = l2projection.cc
if ALBERTA
adapt_SOURCES = adapt.cc
endif
