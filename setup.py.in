from setuptools import setup, find_packages

setup(name="dune.fem",
    namespace_packages=['dune'],
    description="Python lib for dune: fem package",
    version="2.5"
    author="Andreas Dedner and Martin Nolte",
    packages = find_packages(),
    zip_safe = 0,
    package_data = {'': ['*.so']},
    entry_points={
        'console_scripts': [
            'dune-ellipticmodelcompiler = dune.models.elliptic.compiler:main'
            ]}
  )

#    package_data = {'dune': ['${CMAKE_CURRENT_BINARY_DIR}/dune/*.so']}
#    package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
#    packages=[ 'dune.common',
#               'dune.geometry',
#               'dune.grid', ]
